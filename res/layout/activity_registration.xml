<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".RegistrationActivity" >

    <EditText 
        android:id="@+id/et_get_security_code"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/edit_bg"
        android:textSize="18sp"
        android:padding="10dp"
        android:layout_marginTop="15dp"
        android:hint="@string/login_et_user_name"/>
    
    <Button 
            android:id="@+id/btn_get_security_code"
          	android:layout_width="match_parent"
        	android:layout_height="wrap_content"
        	android:text="@string/registration_get_security_code"
         	android:textSize="18sp"
         	android:textColor="#FFFFFF"
         	android:layout_marginTop="15dp"
         	android:layout_below="@+id/et_get_security_code"
         	android:background="@drawable/login_btn_logging" />

</RelativeLayout>
